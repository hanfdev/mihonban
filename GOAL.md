# GOAL：見本盤（mihonban）产品目标与工程边界

本文是当前产品规格。历史阶段计划已经完成或被现行架构取代，不再作为实施依据。

## 一句话目标

为个人稀有音乐收藏提供一套可长期维护的私有曲库：本机可靠整理音频，云端保存元数据并提供多语言网页播放，音源留在用户自己的存储中，不依赖持续运行的家用服务器。

## 产品定位

- 主界面是 `cloud/web` 的 React 播放器，桌面和手机使用同一套体验。
- 主 API 是 `cloud/worker`；生产首选 Cloudflare Workers + D1 + KV。
- D1 是云端专辑、曲目、艺人、收藏、简介、存储绑定的事实来源。
- 音频文件不进入 D1 或 Git；它们保存在 OneDrive、WebDAV、Google Drive，或 Node 部署的本地目录。
- Python 管线负责文件夹和嵌套压缩包入库、乱码修复、标签整理、RYM 本地解析和云同步；它可以按需运行，不是在线播放的依赖。
- 本机播放器是可选工具，不是主产品的运行前提。

## 目标用户与权限

系统服务于一个曲库所有者及其受邀听众：

| 身份 | 能力 |
|---|---|
| 访客 | 仅在管理员显式开放时免密浏览和播放 |
| 听众 | 通过普通口令浏览、搜索、播放和查看收藏 |
| 管理员 | 上传、编辑、隐藏、删除、收藏、导入元数据和管理基础设施 |
| 本机伴侣 | 通过独立 API Key 同步曲库并读取收件箱设置 |

隐藏专辑及其封面、内页、艺人图片、简介和收藏关系只对管理员可见。

## 必须实现的能力

### 曲库与播放

- 专辑、曲目、艺人、收藏视图；搜索、筛选、排序和多语言界面。
- 播放队列、随机、顺序、列表循环、单曲循环、音量持久化和媒体快捷键。
- 正确处理 HTTP Range、MIME、暂停续播和不支持格式。
- OneDrive 优先使用短期直链；无直链的后端由 Worker 流式代理。
- 可选独立音源代理必须有域名白名单和短期签名，不能成为开放代理。

### 元数据

- 专辑、曲目、艺人头像/简介、专辑备注、内页图片和收藏顺序可编辑。
- RYM 只解析用户手动保存的 HTML；不自动访问或抓取 RateYourMusic。
- Discogs 只使用官方 API，并允许导入图片、风格和艺人资料。
- primary/secondary genre 去重并保持顺序。

### 存储

- OneDrive、WebDAV、Google Drive、本地文件夹全部使用统一的命名存储模型。
- Node 运行时额外支持本地文件夹；Cloudflare Worker 不访问主机文件系统。
- 每张专辑和艺人头像都有明确的存储绑定。
- 新上传只写入一个当前写入目标；读取可跨多个后端。
- 迁移先复制并验证，再修改数据库绑定；不自动删除源文件。

### 本机整理

- 文件夹及单层/嵌套 RAR、ZIP、7z 入库，包含 CP932 文件名和 Shift-JIS mojibake 修复。
- MusicBrainz/beets 为主、Discogs 为补充；低置信度项目进入人工队列。
- 入库操作幂等，失败进入隔离区并留日志，不静默丢文件。
- 文件标签尽量保持自描述，换播放器或服务端后仍可识别。

## 数据与备份模型

| 数据 | 位置 | 备份方式 |
|---|---|---|
| 音频与原图 | 用户配置的存储后端 | 存储服务快照或第二份离线副本 |
| 曲库元数据 | D1 或 Node SQLite | `wrangler d1 export` 或仓库导出工具 |
| 云盘/R2/模块设置 | D1 `settings`、`storages` | 管理后台“设置备份”JSON，必须加密保管 |
| 会话、限流、短期下载缓存 | KV | 不迁移，可自动重建 |
| R2 图片镜像索引 | D1 + R2 | 可丢弃并重新预热 |
| 本机标签与 RYM 数据 | 音频 tag、本机 SQLite | 本机备份 |

恢复顺序固定为：部署代码与 schema → 导入 D1 曲库数据 → 导入设置 JSON → 配置 Worker secrets → 抽查播放和图片。

## 推荐架构

```text
浏览器 / PWA
    |
    | HTTPS + 签名会话
    v
Cloudflare Worker + React Assets
    |-- D1：持久元数据
    |-- KV：短期缓存与限流
    |-- R2：可选图片镜像
    |
    +--> OneDrive / WebDAV / Google Drive：音频与原图
    +--> 独立音源代理 Worker：可选、签名、Range

本机 Python 管线 --伴侣 API--> Worker
本机 Python 管线 --rclone/上传--> 音频存储
```

Cloudflare 免费额度适合个人或小范围分享，但不是无限带宽承诺。实际配额以 Cloudflare 当前官方条款为准；大规模公开分发不在目标范围内。

## 安全红线

1. 密钥、refresh token、真实口令、数据库备份和音频不得提交到 Git。
2. 生产环境只使用 HTTPS；`DEV_INSECURE_COOKIE=1` 仅限本机 HTTP。
3. 管理员与听众分权；改管理员口令必须使既有会话失效。
4. 登录失败限流，密码哈希格式异常必须失败关闭。
5. 所有文件路径必须规范化并阻止目录穿越。
6. 隐藏资源的直接 URL 也必须执行权限检查。
7. 外部音源代理必须验证签名、上游协议、域名和每次重定向。
8. RYM 无自动抓取；资源站模块只做只读更新提醒，不自动下载受版权保护内容。

## 可靠性要求

- 数据库 schema 可重复应用；旧库升级通过串行运行时迁移完成。
- 所有外部请求有错误处理，损坏缓存或恢复数据不能导致服务整体崩溃。
- 删除和迁移操作先验证对象存在及关联关系。
- 前端异步请求必须处理路由竞态、失败、重试和组件卸载。
- 桌面与移动视口不得出现控件重叠、裁切或不可操作状态。
- 每次共享行为、权限或存储契约变更都要有相应回归测试。

## 非目标

- 自动抓取或批量下载音乐资源。
- 自动抓取 RateYourMusic。
- 在 Worker 中转码音频或运行常驻本机播放器。
- 面向公众的大规模音乐分发服务。
- 用同步盘目录承载正在写入的本机数据库、临时目录或 `node_modules`。

## 完成定义

一次可发布版本必须同时满足：

- Python 测试、Worker 测试、代理测试和 React 前端生产构建通过。
- Worker 与代理的 Wrangler dry-run 成功，依赖审计无已知漏洞。
- 全新 schema 与旧库运行时迁移均可用。
- 本地和生产式登录、曲库、收藏、艺人、播放接口通过冒烟测试。
- 数据库迁移、设置恢复、存储配置、代理部署和日常操作有可执行文档。
- 工作区不包含密钥、数据库导出、音乐、构建产物或机器专属路径。
